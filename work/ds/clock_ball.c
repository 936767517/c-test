#include<stdio.h>
#include<stdlib.h>
#include<string.h>
#define  NUM 27
struct queue_node{
	int* data;
	int num;
	int front;
	int rear;
	};
struct stack_node{
	int *data;
	int num;
	};
typedef struct queue_node QUEUE;
typedef struct stack_node StACK;
QUEUE queue_create(int num)
{
	QUEUE l=(QUEUE)malloc(sizeof(int)*num);

}
STACK stack_create(int num)
{
	SATCK l=(SATCK)malloc(sizeof(int)*num);	
}
int main(void)
{
	int i;
	QUEUE l
	for
	return 0;	
}
