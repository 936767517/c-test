#include <stdio.h>

#include "itoa.h"

// #include "[/PATH]/xxx.h"

int main(void)
{
	int n;

	scanf("%d", &n);

	itoa(n);

	struct node n1;
	strlen("abc");

	printf("\n%d\n", add(3, 5) );
	printf("\n%d\n", sub(3, 5) );

	return 0;
}
